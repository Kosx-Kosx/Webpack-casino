/**
 * Date range filter options used for quick filtering in Statements and Game Sessions
 */
export enum DateRangeFilter {
  TODAY,
  YESTERDAY,
  THIS_WEEK,
  LAST_FIFTEEN_DAYS,
  THIS_MONTH,
  CUSTOM,
}
